//*******************************************************
// ShoreSide Configuration File

ServerHost = localhost
ServerPort = $(VPORT)
Community  = $(VNAME)

#include plug_origin_warp.moos
  
//--------------------------------------------------------
Processconfig = ANTLER
{
  MSBetweenLaunches = 200

  Run = MOOSDB          @ NewConsole = false
  Run = pRealm          @ NewConsole = false
  Run = pMarineViewer   @ NewConsole = false
  Run = pLogger         @ NewConsole = false
  Run = pShare          @ NewConsole = false
  Run = pHostInfo       @ NewConsole = false
  Run = uFldShoreBroker @ NewConsole = false
  Run = pRealm          @ NewConsole = false
  Run = uTimerScript    @ NewConsole = false
  Run = pPointAssign    @ NewConsole = false
}

#include plug_pShare.moos
#include plug_pHostInfo.moos
#include plug_pLogger.moos

//--------------------------------------------------
// uFldShoreBroker Configuration Block

ProcessConfig = uFldShoreBroker
{
  AppTick       = 1
  CommsTick     = 1

  qbridge  = DEPLOY, NODE_REPORT
  qbridge  = STATION_KEEP, APPCAST_REQ
  qbridge  = MOOS_MANUAL_OVERRIDE

  bridge   = src=RETURN_ALL, alias=RETURN
  bridge   = src=RETURN_$V,  alias=RETURN
  bridge  = src=REFUEL_NEEDED_ALL
  bridge = src=VISIT_POINT_$V, alias=VISIT_POINT
  bridge  = src=WPT_UPDATE_$V, alias=WPT_UPDATE
  //bridge = src=READY_TO_RECEIVE_$V
}

//--------------------------------------------------
 // pRealm Configuration Block  

ProcessConfig = pRealm                                  
    {                                                               
      AppTick   = 4                                                 
      CommsTick = 4                                                 
                                                                   
      relcast_interval = 0.8       //  [0.4, 15] Default is 0.8     
      summary_interval = 2.0       //  [1, 10] Default is 2         
      wrap_length = 90             //  [1,inf] Default is 90        
      trunc_length = 270           //  [1,inf] Default is 270       
      msg_max_history = 10         //  [1,inf] Default is 10        

        hist_var = VISIT_POINT  
        hist_var = VISIT_POINT_GILDA
        hist_var = VISIT_POINT_HENRY
  } 
//--------------------------------------------------------
ProcessConfig = pMarineViewer
{
  AppTick    = 4
  CommsTick  = 4

  tiff_file            = forrest19.tif
  set_pan_x            = -90
  set_pan_y            = -280
  zoom                 = 0.65
  vehicles_shape_scale = 1.5
  vehicles_name_mode   = names+shortmode

	nodes_font_size = xlarge
	procs_font_size = xlarge
	appcast_font_size = large
	
  point_viewable_labels   = false
  polygon_viewable_labels = false
  appcast_viewable     = true
  appcast_color_scheme = indigo
  hash_delta           = 50
  hash_viewable        = true
  hash_shade           = 0.35

  scope = PHI_HOST_INFO

  button_one   = DEPLOY  # DEPLOY_ALL=true 
  button_one   = MOOS_MANUAL_OVERRIDE_ALL=false 
  button_one   = RETURN_ALL=false # STATION_KEEP_ALL=false

  button_two   = RETURN  # RETURN_ALL=true
  button_two   = STATION_KEEP_ALL=false

  button_three = STATION  # STATION_KEEP_ALL=true

  button_four   = REFUEL # REFUEL_NEEDED_ALL=true

  action = REFUEL_NEEDED_ALL = true //Accept refueling command
}

//--------------------------------------------------------
ProcessConfig = uTimerScript
{
  AppTick = 4
  CommsTick = 4

 // event = quit
  block_on = pPointAssign

  paused = false
  // pause_var = POINT_ASSIGN_START

  rand_var = varname=RND_VAL, min=-175, max=-25, key=at_post, snap=1
  rand_var = varname=RND_X, min=-25, max=200, key=at_post, snap=1
  
  event = var=VISIT_POINT, val="firstpoint", time=0
  event = var=VISIT_POINT, val="x=$[RND_X], y=$[RND_VAL], id=$[TCOUNT]", time=5, amt=100
  event =var=VISIT_POINT, val="end", time=10
}

ProcessConfig = pPointAssign
{
  AppTick    = 4
  CommsTick  = 4

  region_assign = false
  x_divider = 87.5
}
